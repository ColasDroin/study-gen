# Generate and write particle distribution
gen1:
  script:
    prepare_mad_environment:
      args: links
      output: mad_b1b2, mad_b4
    build_hllhc_sequence:
      args: [mad_b1b2, apply_acsca_fix, cycle_to_IP3, incorporate_CC, beam = 1]
      output:

  # parameters:
  #   a:
  #     range: [1, 100]
  #     steps: 3
  #   b:
  #     range: [10, 20]
  #     steps: 2
